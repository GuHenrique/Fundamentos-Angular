import { MultiplicaPorPipe } from './mulpica-por.pipe';

describe('MulpicaPorPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiplicaPorPipe();
    expect(pipe).toBeTruthy();
  });
});
